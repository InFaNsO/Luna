using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controller_Enemy : MonoBehaviour
{
    //public void Attack(ref Enemy enemy)
    //{
    //    enemy.Attack();
    //}

    //public void MoveLeft(ref Enemy enemy)
    //{
    //    enemy.transform.Translate(-enemy.GetMoveSpeed(),0f,0f);
    //}

    //public void MoveRight(ref Enemy enemy)
    //{
    //    enemy.transform.Translate(enemy.GetMoveSpeed(), 0f, 0f);
    //}

    //public void Jump(ref Enemy enemy)
    //{
    //    enemy.transform.Translate(0f, enemy.GetJumpStrength(), 0f);
    //}


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
